% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tula.R, R/tula_clm.R, R/tula_coxph.R,
%   R/tula_fixest.R, R/tula_glm.R, R/tula_lm.R, R/tula_multinom.R,
%   R/tula_negbin.R, R/tula_polr.R, R/tula_rq.R, R/tula_survreg.R
\name{tula}
\alias{tula}
\alias{tula.default}
\alias{tula.data.frame}
\alias{tula.clm}
\alias{tula.coxph}
\alias{tula.fixest}
\alias{tula.fixest_multi}
\alias{tula.glm}
\alias{tula.lm}
\alias{tula.multinom}
\alias{tula.negbin}
\alias{tula.polr}
\alias{tula.rq}
\alias{tula.rqs}
\alias{tula.survreg}
\title{Stata-Inspired Regression and Summary Output}
\usage{
tula(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  sep = 5L,
  mad = FALSE,
  median = FALSE,
  digits = 7L,
  exp = FALSE,
  level = 95,
  parallel = FALSE,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  codebook = FALSE,
  ...
)

\method{tula}{default}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  sep = 5L,
  mad = FALSE,
  median = FALSE,
  digits = 7L,
  exp = FALSE,
  level = 95,
  codebook = FALSE,
  ...
)

\method{tula}{data.frame}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  sep = 5L,
  mad = FALSE,
  median = FALSE,
  digits = 7L,
  exp = FALSE,
  codebook = FALSE,
  ...
)

\method{tula}{clm}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  exp = FALSE,
  level = 95,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  ...
)

\method{tula}{coxph}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  exp = TRUE,
  level = 95,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  ...
)

\method{tula}{fixest}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  exp = FALSE,
  level = 95,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  ...
)

\method{tula}{fixest_multi}(model, ...)

\method{tula}{glm}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  exp = FALSE,
  level = 95,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  ...
)

\method{tula}{lm}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  exp = FALSE,
  level = 95,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  ...
)

\method{tula}{multinom}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  exp = FALSE,
  level = 95,
  parallel = FALSE,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  ...
)

\method{tula}{negbin}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  exp = FALSE,
  level = 95,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  ...
)

\method{tula}{polr}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  exp = FALSE,
  level = 95,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  ...
)

\method{tula}{rq}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  exp = FALSE,
  level = 95,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  ...
)

\method{tula}{rqs}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  exp = FALSE,
  level = 95,
  parallel = FALSE,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  ...
)

\method{tula}{survreg}(
  model,
  wide = NULL,
  ref = FALSE,
  label = TRUE,
  width = NULL,
  exp = FALSE,
  level = 95,
  robust = FALSE,
  vcov = NULL,
  cluster = NULL,
  ...
)
}
\arguments{
\item{model}{A fitted model object or a data frame / vector. Supported
model classes: \code{lm}, \code{glm}, \code{negbin} (negative binomial via
\code{\link[MASS:glm.nb]{MASS::glm.nb()}}), \code{multinom} (multinomial logit via \code{\link[nnet:multinom]{nnet::multinom()}}),
\code{polr} (ordered regression via \code{\link[MASS:polr]{MASS::polr()}}), \code{clm} (ordered regression
via \code{\link[ordinal:clm]{ordinal::clm()}}), \code{coxph} (Cox proportional hazards via
\code{\link[survival:coxph]{survival::coxph()}}), \code{rq} and \code{rqs} (quantile regression via
\code{\link[quantreg:rq]{quantreg::rq()}}), \code{fixest} (fixed effects models via \code{\link[fixest:feols]{fixest::feols()}},
\code{\link[fixest:feglm]{fixest::feglm()}}, \code{\link[fixest:feglm]{fixest::fepois()}}, \code{\link[fixest:femlm]{fixest::fenegbin()}}),
\code{survreg} (tobit / survival regression via \code{\link[AER:tobit]{AER::tobit()}} or
\code{\link[survival:survreg]{survival::survreg()}}). When passed a data frame or atomic vector,
produces descriptive statistics output.}

\item{wide}{Logical or \code{NULL}. If \code{TRUE}, 95\% confidence interval columns
are added. If \code{FALSE}, they are omitted. If \code{NULL} (the default),
confidence intervals are shown automatically when the effective output
width is 80 or more, and omitted when it is narrower.}

\item{ref}{Logical. If \code{FALSE} (the default), the reference category of
each factor variable is omitted. If \code{TRUE}, it is shown as the last row
within its group, with a coefficient of 0 and blanks for SE, statistic,
and p-value.}

\item{label}{Logical. If \code{TRUE} (the default), value labels are used for
factor levels when available (e.g. from \code{haven}-labelled variables).
If \code{FALSE}, raw level values are always used.}

\item{width}{Integer or \code{Inf}. Maximum total character width of the output.
Defaults to \code{getOption("width")} (typically 80 in an interactive R
session, but respects whatever the user has set). When the natural width
of the table or header would exceed this value, the label column is
narrowed and long labels are truncated with a trailing \code{~}. Set to \code{Inf}
to allow unlimited width.}

\item{sep}{Integer. For summarize output: number of variables between each
horizontal separator line. Default 5. Set to 0 to suppress separators.
Separators never split a factor variable's level rows; if a sep boundary
falls inside a factor block, the separator is placed after the block.
Ignored for regression output.}

\item{mad}{Logical. For summarize output: if \code{TRUE}, show the mean absolute
deviation (MAD) instead of the standard deviation. Column header changes
to \code{"MAD"}. Ignored for regression output.}

\item{median}{Logical. For summarize output: if \code{TRUE}, show the median
instead of the mean and the IQR instead of SD/MAD. Column headers change
to \code{"Median"} and \code{"IQR"}. Factor variable display is unchanged.
Ignored for regression output.}

\item{digits}{Integer. For summarize output: number of significant digits
used when formatting numeric values (mean, SD/MAD/IQR, min, max).
Default 7, which avoids scientific notation for numbers up to 9,999,999.
Ignored for regression output (which always uses 4 significant digits).}

\item{exp}{Logical. If \code{TRUE}, coefficients are exponentiated: the estimate
column shows \code{exp(b)} and the standard error column shows the delta-method
standard error (\code{exp(b) * SE(b)}), labelled \code{"DMSE"}. The test statistic
and p-value are unchanged. Reference-level rows show \code{1} instead of \code{0}.
When \code{wide = TRUE}, CI bounds are also exponentiated.
Default \code{FALSE}. Ignored for summarize output.}

\item{level}{Numeric. Confidence interval width as a percentage (e.g. 90,
95, 99). Values less than 1 are multiplied by 100 (so \code{0.95} becomes 95).
Default 95. Ignored for summarize output.}

\item{parallel}{Logical. For multinomial logit and multi-quantile models:
if \code{TRUE}, outcomes/quantiles are shown as side-by-side columns with
significance stars instead of stacked blocks. Default \code{FALSE}.}

\item{robust}{Logical. If \code{TRUE}, heteroskedasticity-robust (HC3) standard
errors are used. Requires the \code{sandwich} package. Default \code{FALSE}.}

\item{vcov}{Character or matrix. When a character string (e.g. \code{"HC4"}),
specifies the HC type for robust SEs. When a matrix, used directly as
the variance-covariance matrix. Default \code{NULL}.}

\item{cluster}{Character. Variable name for cluster-robust standard errors.
Implies \code{robust = TRUE}. Default \code{NULL}.}

\item{codebook}{Logical. For data frames and vectors: if \code{TRUE}, produces
Stata-inspired codebook output (per-variable blocks with type, range,
unique values, and tabulations or percentile distributions) instead of
the default summary table. Default \code{FALSE}. Ignored for regression output.}

\item{...}{Additional arguments passed to model-specific methods.}
}
\value{
For regression models, invisibly returns a \code{tula_output} object.
For data frames and vectors, invisibly returns a \code{tula_summary} object.
The primary side effect is printing to the console.
}
\description{
\code{tula()} is an S3 generic that produces Stata-inspired console output for
two distinct input types: regression models (coefficient table with header
block) and data frames or vectors (descriptive statistics table).
}
\details{
For regression models, the output includes a two-block header (model fit
statistics on the left, sample statistics on the right) and a coefficient
table with optional confidence intervals. Factor variables are grouped:
the variable name appears on its own header row, and each non-reference
level is indented below it. The intercept is always placed last.
}
\examples{
m <- lm(mpg ~ cyl + wt, data = mtcars)
tula(m)
tula(m, wide = TRUE)
tula(m, width = 120)

m2 <- glm(am ~ cyl + wt, data = mtcars, family = binomial)
tula(m2)

tula(mtcars)
tula(mtcars, sep = 3, median = TRUE)

}
