% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tulatab.R
\name{tulatab}
\alias{tulatab}
\title{Stata-Inspired Frequency Tabulation}
\usage{
tulatab(
  y,
  x = NULL,
  data = NULL,
  missing = FALSE,
  sort = FALSE,
  value = TRUE,
  label = TRUE,
  width = NULL,
  pct = "col",
  freq = TRUE,
  by = NULL,
  mean = NULL,
  var = NULL,
  stat = NULL,
  listwise = TRUE,
  dec = NULL,
  ...
)
}
\arguments{
\item{y}{A vector (factor, character, numeric, or haven-labelled) or an
unquoted variable name when \code{data} is supplied.  In two-way mode,
this is the row variable.}

\item{x}{Optional.  A second vector or unquoted variable name for the
column variable.  When supplied, a two-way crosstab is produced.}

\item{data}{Optional data frame.  When supplied, \code{y} and \code{x}
are evaluated inside \code{data} using non-standard evaluation.}

\item{missing}{Logical.  If \code{FALSE} (default), \code{NA} values are
excluded.  If \code{TRUE}, missing values appear as extra rows (and
columns in two-way mode).}

\item{sort}{Logical.  If \code{TRUE}, rows (and columns in two-way mode)
are sorted by descending marginal frequency.}

\item{value}{Logical.  For haven-labelled variables: show numeric codes.
In two-way mode, when both \code{value} and \code{label} are
\code{TRUE}, labels take precedence (codes hidden).}

\item{label}{Logical.  For haven-labelled variables: show value labels.}

\item{width}{Integer, \code{Inf}, or \code{NULL}.  Maximum total character
width.  Defaults to \code{getOption("width")}.}

\item{pct}{Character or \code{NULL}.  Two-way mode only.
\code{"col"} (default) = column percentages,
\code{"row"} = row percentages,
\code{"cell"} = cell percentages,
\code{NULL} = no percentages.}

\item{freq}{Logical.  Two-way mode only.  If \code{TRUE} (default),
frequencies are shown.}

\item{by}{Optional.  A vector or unquoted variable name for a grouping
variable.  When supplied (two-way mode only), a separate crosstab is
produced for each level of the grouping variable.  For haven-labelled
\code{by} variables, panel headers show \code{"name = code [label]"}.}

\item{mean}{Optional.  A numeric vector or unquoted variable name.  When
supplied, replaces frequencies and percentages with the mean and
non-missing count (N) of this variable per category (one-way) or per
cell (two-way).  In one-way mode, supports multiple variables via
\code{mean = c(mpg, wt)}.  The \code{pct} and \code{freq} parameters
are silently ignored in two-way mode when \code{mean} is specified.}

\item{var}{Optional.  Synonym for \code{mean}.  Specify one or the other,
not both.}

\item{stat}{Optional character string.  One-way mode only.  A
space-separated list of summary statistics to display per category
instead of the default Mean + N columns.  Requires \code{mean} or
\code{var} to identify the variable.  Valid tokens: \code{"mean"},
\code{"median"}, \code{"sd"}, \code{"n"}, and percentiles
\code{"p1"}\dots\code{"p99"} (e.g. \code{"p25"}, \code{"p75"}).
Example: \code{stat = "mean sd p25 p75"}.  Ignored with a warning in
two-way mode.}

\item{listwise}{Logical (default \code{TRUE}).  When displaying multiple
\code{mean}/\code{var} variables in one-way mode, should observations
missing on \emph{any} variable be excluded from \emph{all} columns?
When \code{FALSE}, each variable column uses all of its own non-missing
observations independently.}

\item{dec}{Optional integer.  Number of decimal places for displayed
values.  Applies to mean-mode and stat-mode numeric output.  When
\code{NULL} (default), formatting uses the standard smart-rounding
rules.}

\item{...}{Reserved for future extensions.}
}
\value{
Invisibly returns a \code{tula_tab} (one-way),
\code{tula_crosstab} (two-way), or \code{tula_crosstab_by} (grouped
two-way) S3 object.
}
\description{
Produces a Stata-inspired one-way frequency table or two-way
crosstabulation.
}
\details{
When called with one variable, produces a one-way frequency table with
counts, percentages, and (for ordinal / numeric variables) cumulative
percentages. When called with two variables, produces a two-way
crosstabulation with cell frequencies and column (or row / cell)
percentages. In either mode, the optional \code{mean} parameter replaces
frequencies and percentages with the mean and non-missing count of a
numeric variable.
}
\examples{
tulatab(mtcars$cyl)
tulatab(cyl, data = mtcars)
tulatab(mtcars$vs, mtcars$cyl)
tulatab(vs, cyl, data = mtcars, by = am)

}
